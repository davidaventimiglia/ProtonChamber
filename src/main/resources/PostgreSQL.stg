getEntityCollection_select(table, tables, keys, limit) ::= "with t as (select <table>.* from <tables> where <keys> <limit>) select * from t"

getEntityCollection_count(tables, keys) ::= "with t as (select count(1) from <tables> where <keys>) select * from t"